# 市场观察视频模板 V2.1

本模板用于制作“市场观察”频道的财经、加密、美股、监管、交易所风险、产业链研究类视频。默认流程是：先按同目录 `标题V2.md` 生成 `3` 组一一对应的 Shorts/长视频标题，选定一组后研究事实并分别编写两条脚本，再生成可看的 HTML 演示、男声配音和正式 MP4；最终同时交付 Shorts 与长视频的标题、说明、标签、关键词和缩略图。

工程化动画规则于 `2026-09-03` 吸收 Hyperframes 的 HTML 原生、可寻址时间轴、逐帧确定性、媒体轨和渲染验收方法。新项目在本机可用时，必须直接调用同目录的 `财经视频组件库/six-section-template`；框架不可用时才实现等价契约。这不改变事实核验、频道边界、固定背景图库和“禁止凑脚本”规则。

## 0.0 Shorts 钩子 → 长视频承接（强制）

以后默认一个选题制作两条内容：

- `Shorts`：负责吸引新观众、给出最小完整结论、留下一个具体且真实的待展开问题。
- `长视频`：负责完整数据、机制、反方解释、风险边界和可执行观察方法。

制作前必须先执行 `标题V2.md`：一次给出 `3` 组候选，每组包含 `Shorts 标题 + 长视频标题 + 对应关系 + Shorts 结尾引导`。两条标题必须围绕同一对象和同一核心问题，但不得逐字重复。用户没有明确选择时，按事实强度、频道匹配和预期留存选出首选继续制作，并保留另两组备选。

Shorts 不是长视频的机械裁切版。两条内容可以共用 `product-facts.md`、证据截图、数据和品牌资产，但必须分别制作脚本、时间轴、视觉计划和成片。默认先让长视频可访问，再发布 Shorts，并把 Shorts 的“相关视频”、说明和置顶评论统一指向对应长视频。

## 0. 合成前增长验收门槛（强制）

每期必须创建 `preflight.md`，按第0.2节分阶段记录“待检查 / 通过 / 不通过、证据、修改动作”。脚本门通过后才生成男声，有声演示门通过且用户确认后才正式渲染，成片与发布门通过后才交付可上传；当前阶段强制项未通过不得进入下一阶段，禁止预填尚未产生的成片验收结果。

### A. 选题与时效

- 标题必须说清对象、核心问题和观众收益；禁止只用“到底了吗”“怎么看”“最新解读”等脱离对象的泛标题。
- 当前市场题必须有可核验的资料截止日期；核心指标须写明最新可见日期。数据已经过期、只能使用教学案例时，不能包装成“当前行情判断”。
- **禁止标记当天时间（强制）**：公开标题、缩略图、片头、画面角标、字幕、旁白、片尾和上传说明中，不得出现制作日、上传日或“今天 / 今日 / 截至今天 / 本期录制于”等当天时间标记，也不得用当天日期制造时效感。研究底稿、`preflight.md`、来源记录和内部元数据仍须保留精确读取时间与资料截止日期。事件发生日、财报季度、政策生效日、统计区间等理解事实所必需的日期可以保留，但不能伪装成制作日或上传日。
- 冷门指标必须先翻译成观众问题。例如“亏损供应量”应对应“抄底信号还是陷阱”，不能只把指标名称当标题。
- 热门标题、社交媒体传闻和推荐视频标题不是事实来源；未经独立确认的数字、爆仓人数、因果关系不得写入标题、缩略图或旁白。

### B. 标题与缩略图

- 标题与缩略图必须共同回答：讨论什么、为什么现在值得看、视频会给出什么判断或框架。
- 缩略图必须在手机小尺寸下可读：只保留 `1` 个对象、`1` 个冲突或数据点、`3—6` 个高对比短字；不能像课程封面、报告封面或大面积留白的静态 PPT。
- 缩略图文字不能只复述标题；必须补充标题未写出的冲突、数据或结论。
- 提交 `1280×720` 缩略图和约 `320px` 宽的小图预览。小图中看不清对象或短句，即为不通过。

### C. 前 30 秒与承诺兑现

- `0—15` 秒必须给出结论、冲突或关键事实，不能只播放静态标题页、频道名或概念定义。
- `0—30` 秒必须明确回答标题提出的问题，并说明接下来用哪 `2—4` 个证据验证；不能让观众等待到中后段才知道视频价值。
- 标题承诺必须在前 `30` 秒兑现方向性答案；若内容只能解释概念，标题必须改为入门/教程型，不能伪装为即时判断。
- 演示验收时抽看 `00:00:05`、`00:00:15`、`00:00:30`，确认画面、字幕和旁白均围绕同一个核心问题。

### D. 节奏、画面与数据

- 单一底色、同一网格、同一个图标或同款卡片不能贯穿全片；每个新章节、新对象、新数据或新判断必须切换为对应的视觉或明显动效。
- 数据解释视频不得只用概念卡。至少包含 `1` 张真实数据图、`1` 张机制图、`1` 张风险/反例图；所有真实数据必须与 `product-facts.md` 对应。
- 关键事实、判断与行动 cue 必须有“前景证据层”：数据卡、机制图、对比关系、流程步骤、来源摘录的重制版或自制示意图至少 `1` 项。自然背景只负责情绪与留白，不能单独承担信息表达；连续超过 `8` 秒仅有背景与字幕，视为不通过。
- **连续动态视觉对象（强制）**：每条长视频必须从本期核心问题中选定 `1—2` 个可识别的 Hero 对象，并让其跨至少 `3` 个章节连续出现和改变状态。Hero 对象可以是同一只硬件钱包、股票本体/链上凭证、订单、资金流、监管文件、风险入口或用户决策路径；不得只是复用同一张大卡片、同一张静态曲线或同一背景。每次状态变化必须与旁白新增事实、机制、风险或行动对应，例如“股票本体 → 托管凭证 → 可否赎回”、“订单信息 → 用户画像 → 诈骗入口”、“资金流 → 估值传导 → 风险过滤”。
- Hero 对象必须有稳定 ID、初始状态、章节状态和终态；至少出现 `3` 次可见的状态变化，变化可使用位置、比例、连线、遮罩、数据、标签、对象关系或状态色。只在章节开头淡入一次、随后整页静止超过 `12秒`，或只让文字替换而对象无变化，均视为不通过。
- 固定背景图库中的已选背景必须在最终合成画面中真实可见。若组件、全屏纯色板、蒙版或不透明容器将其完全盖住，则该背景不算已使用，必须调整透明度、裁切、景深层、遮罩或版式；不能在 `visual-plan.md` 写了背景路径、实际成片却只剩同一种底色。
- 每张关键画面遵循“一屏一结论”：`1` 个主判断 + `1` 个可核验数据/证据 + 最多 `3` 个辅助标签。结论、证据和辅助信息必须有明显字号层级，不能把旁白逐字堆成满屏文字。
- 字幕不得超过 `2` 行，不能长时间覆盖核心图表；底部 `260px` 安全区内不得摆放主视觉或关键数据。
- **画面减法（强制）**：正式视频禁止右上角章节编号、页码、日期或计数器，禁止右下角常驻来源角标，禁止底部进度条和无必要的角落提示。默认只保留频道品牌、主视觉/证据板和字幕。
- **无重影转场（强制）**：旧场景容器先隐藏，新场景单独入场；跨章节Hero使用独立连续轨，不复制成双份。字幕不得交叉叠加。正式渲染关闭依赖真实时钟自动播放的CSS动效，但保留由Hyperframes/确定性seek控制的位移、缩放和揭示动画；禁止用全片冻结代替无重影验收。
- 生成接触表或抽帧集，至少检查开头、`30` 秒、`50%` 时点和结尾；若画面同质化、文字过密或与旁白不符，即为不通过。
- 除常规接触表外，必须为每个 Hero 对象生成“状态连续性抽帧组”：至少抽取它在 `3` 个不同章节的画面，并在 `preflight.md` 写明“对象身份 / 初始状态 / 中间状态 / 终态 / 对应旁白”。仅能证明字幕或整页卡片变化，不能证明对象状态变化，视为不通过。

### E. 发布前决策

- 新视频若只是旧片换标题，须先提出旧片标题/缩略图的单变量测试方案，经用户授权后实施；`48—72小时` 仅作初次观察窗口，曝光不足不能据此认定无效或自动重做。事实错误优先修正，不等待增长实验。
- 需要重做时，必须改变至少 `2` 项：时效数据、核心问题、前 `30` 秒、视觉结构或时长；禁止原样重复上传。
- 发布后记录展示、CTR、前 `30` 秒留存、平均观看比例、观看时长和订阅变化。数据不足时明确写“样本不足”，不得把单个小样本当结论。

## 0.2 实片复盘整改契约（强制，优先于冲突旧条款）

依据：`video-template-review/audit-2026-09-05/最近10条长视频复盘.md`。本节固化的是可验证的制作要求，不把本批小样本点击率、观看时长当作平台达标线。适用于后续新作和用户要求的重制，不自动修改或删除已发布内容。

### A. 分阶段阻断与证据

- `preflight.md` 按“脚本门 / 有声演示门 / 正式成片门 / 发布门”分别验收；每项记录检查对象、实际证据路径与时间点、状态、问题、修复动作和复查结果。未检查一律为“待检查”，不能预填“通过”；不适用须说明理由。
- 脚本门通过后才生成配音和有声演示；演示门通过且用户确认后才正式渲染；正式成片门和发布门通过后才交付为“可上传”。成片尚未产生时，不得要求其检查预先通过，也不得借此跳过后续验收。
- 音轨、字幕、数据、布局或封面修改后，相应验收失效，必须重新检查实际输出。HTML通过不等于MP4通过；报告必须说明完整检查、抽查和未检查的范围。
- 必交 `opening-proof.md`、`caption-audit.md`、`visual-plan.md`、`publish-check.md` 及抽帧证据；可由工具生成，但不得仅凭程序返回成功或模板规则存在就宣称验收通过。

### B. 开场兑现与时长

- 前 `20—30秒` 必须展示至少 `1` 项与标题直接相关的可核验证据、具体示例或可执行判断；只说“先给结论”“这期讲4件事”不算兑现。新闻事实无法核验时改为明确的教学示例，不得虚构新闻。
- `opening-proof.md` 写明标题承诺、第一项证据/动作、来源、出现时间和对应帧；抽查 `5 / 15 / 30秒`。禁止用目录、频道介绍和重复免责声明占满开场。
- 资金主题先展示主要资金分配或现金流桥；安全主题先展示关键危险入口与停止条件；教程先展示要完成的任务或结果；人物案例使用片段时核对素材使用依据，保留“单方自述”等必要边界，并立即衔接核验方法。
- 不强制把所有长片剪短，也不为达到时长凑内容。每段必须新增证据、解释、示例或行动；重复结论、重复风险提示与无关宏观铺垫删除或合并。合法性、风险与证据边界放在相关论断处，不得为节奏牺牲准确性。

### C. 真实音频驱动的短句字幕与配音复核

- 配音按完整语义自然连读；字幕另行切成短句，不把整段TTS chunk直接显示为字幕。每屏最多 `2行`，每行约 `14—20个汉字` 为起始设计目标；英文和金额按实际宽度判断，词组、币种和金额不得从中间拆断。禁止用缩小字号规避超长。
- 字幕时间必须来自最终音频的字词边界、强制对齐或人工校准；禁止用字数比例均分、脚本估时作为最终同步依据。字幕采用独立 `start/end` 区间并覆盖真实停顿，不能因分段累积遗漏间隔造成漂移；长停顿可留空，不必把上一句一直挂到下一句。
- `caption-audit.md` 记录对齐方法及版本，逐项检查开头、最长字幕、关键数字/术语、各章边界、中段、结尾。必须实时播放核对音画；只抽静帧不能证明同步。未完成听审不得写“读音全部正确”。
- TTS替换后必须扫描最终配音输入，而非只看源脚本；阻断“微策略，原名微策略”式重复、重复单位、遗留反引号和错误缩写替换。关键金额和专名必须试听；配音重生成后重建字幕时间轴。
- 保留底部字幕安全区、无白色字幕底板、无重影与既有读音规则。将最终画面等比缩为约 `360px` 宽实查，字幕不需放大才可阅读；不通过则重排、精简或放大，不得标记为手机适配。

### D. 证据主导画面与有意义的运动

- 固定图库、随机选择器和同日全局去重继续执行，已选背景应实际可见；但背景可见不等于风景必须占据视觉中心。与“背景成为主画面”理解冲突时，以前景证据可读和本段解释有效为准。
- `visual-plan.md` 每段必须回答“观众通过画面比只听旁白多理解什么”。资金流、价格区间、风险状态、操作界面等放大到可读，禁止用山景、品牌发光球、装饰粒子或无数据曲线代替论证。
- 连续Hero对象和Hyperframes规则保留；对象必须推进资金去向、因果关系、数据焦点或行动状态。只换背景、整页标题或数字不算连续叙事。主数据变化由可寻址时间轴驱动，不能为避免重影把所有有效动画冻结。
- 实操承诺必须有实际界面或明确标记的示范界面及完整步骤；真实数据图必须有来源、单位、区间，示意图不能伪装成行情。默认不以文件大小、码率或动画数量判定质量。
- 检查静态可读性与转场两类证据：最长字幕、关键数据、各章节及边界前后帧、结尾；不得遮挡、贴边、残留cue ID、页码、调试占位符或无必要角标。

### E. 缩略图手机验收

- 封面只表达 `1个主体 + 1个核心矛盾 + 至多1组关键数据`；主体必须让目标观众认出本期对象，品牌不应只藏在小角标。标题和封面互补，不复述整句。
- 禁止完整榜单、多行财报、长流程和目录塞进封面；移到正文逐项呈现。金属立体效果必须保持实色字面、高对比和清楚轮廓，不得暗金压黑底或细描边导致难读。
- 长视频输出 `1280×720` 与约 `320px` 宽预览，Shorts另按竖屏安全区检查。预览无法快速识别对象、冲突和短字即阻断发布；“精致”“立体”不是豁免条件。

### F. 数据与教程前提

- `product-facts.md` 逐项登记原始值、来源URL、来源等级、事件/统计时点、时区、单位、中文换算和计算式。未收盘数据不能称收盘价；区间涨幅必须核对起点、终点、完成状态和分母。
- “唯一 / 史上 / 全部 / 首次”等必须给适用样本、截止范围及证据；榜单内特征不得推广成全市场历史。总强平金额不能证明全部为空单或多单，更不能单凭共时发生确定因果。
- 公司公告、媒体报道、独立报告和已取得的审计原文必须区分；免责声明不能把未经核实的确定性标题变合格。旧脚本写“已确认”不等于本轮验证。
- 教程先划清对象与例外。例如原生ETH没有ERC20代币合约地址，WETH、跨链映射资产和同名代币必须分别核验；不能把“没有合约地址”写成原生ETH是假币的依据。
- 数据矛盾、算式不符、错误操作指导属于P0阻断项，必须先修内容再优化点击包装。

### G. 发布物料拆分

- 分别输出 `youtube-description-long.txt`、需要时的 `youtube-description-shorts.txt`、`youtube-tags.txt`、`youtube-keywords.txt`；汇总文档仅作内部索引，不得把整份工作稿当说明粘贴。
- 对外说明只含主题价值、章节、具体来源链接与必要边界；移除反引号、Markdown栏目符、内部指令、占位内容及另一版本的说明。标签填写对应后台字段，关键词用于研究，不向说明堆词。
- 章节时间必须匹配最终MP4；标签强相关，不把BTC标签套到无关公司财报。发布前检查正确文件名、版本、横竖屏、缩略图及说明配对。
- 发布链接未知时内部交付可明确提醒待补，但公开说明不得含“发布后替换”等工作占位符或虚构URL。最终会话仍须直接交付用户要求的标题、说明、标签、关键词、封面和绝对路径。

### H. 复盘与优化决策

- 建立 `performance-review.md`，区分曝光不足、点击不足、进入后停留短三类；曝光相对充分而点击低优先测试标题封面，点击后停留短先检查开场兑现、字幕与内容，不默认全部重做。
- 记录统计窗口、发布/修改时间、流量来源、展示、点击率、观看时长、可用留存和数据处理状态。按首24小时/首7日等可比窗口及同类来源比较；缺失留存写缺失，不以平均观看时长冒充精确退出点。
- 小样本不得判定封面成功、内容失败、限流或全频道转型；累计观看不得无条件代表独立人数。后台口径变化须注明，不混用实时卡和高级表，不机械用观看数乘平均时长复算。
- 一次尽量只改变一个主要变量并登记；经过48—72小时但曝光不足仍为“无法判断”。不因低播放自动删片重传，不刷自播，不承诺流量。准确性错误无需等待增长实验，立即给出修正建议；改后台、删除或重传需用户授权。
- 每次复盘必须区分实查事实、待验证原因与优化假设；建议须附依据、风险和替代解释，不迎合用户或只追求更夸张包装。

## 1. 触发口令

用户出现以下表达时，默认启用本模板：

- `按财经视频引擎做`
- `模板财经视频引擎`
- `市场观察视频模板`
- `按财经视频引擎V2做`
- `财经视频引擎V2`
- `做个视频`
- `先脚本/演示，我确认后生成 MP4`
- `男声配音`
- `上传 YouTube`

如果用户只说“继续”，以上一轮正在制作的财经视频引擎项目为准继续执行。

## 2. 默认视频规格

- 长视频比例：`16:9`
- 长视频分辨率：`1920×1080`
- Shorts 比例：`9:16`
- Shorts 分辨率：`1080×1920`
- 帧率：`25fps`
- 编码：`H.264 + AAC`
- Shorts 默认时长：`25—45 秒`；证据充分的叙事题可放宽到 `60 秒`，超过 `60 秒` 必须说明必要性
- 长视频默认时长：`5—6 分钟`
- 深度研究类可放宽到：`6—8 分钟`
- 超过 `8 分钟` 必须明确提醒用户，并说明原因；如用户要求节奏更紧，要压缩到 `6分30秒—7分30秒`
- 配音：中文男声，默认 `zh-CN-YunyangNeural`
- 语速：市场快讯默认 `+3%`；新手教程、概念课和长篇操作指南默认正常语速 `+0%`，不得为压缩时长加速
- 音高：默认 `-2Hz`
- 最终 MP4：不能出现“演示”“继续播放”“含声音”“测试版”等制作过程字样

## 2.1 时长与信息密度（强制，禁止凑脚本）

- 时长是研究素材和有效信息密度的结果，不是先定 `5—6 分钟` 再用旁白填满的目标。默认时长仅为充足材料下的参考范围。
- 每一个 cue 必须至少新增 `1` 项有效内容：可核验事实或数据、因果机制、对象间对比、反例/风险、情景边界，或可执行的观察动作。只换说法重复前文结论的 cue 必须删除。
- 禁止用空泛宏观感叹、无来源的市场情绪、重复风险提示、重复三段式总结，或与核心问题无关的历史背景来延长时长。
- 资料不足时，必须收缩为完整的短片（通常 `2—4 分钟`）、补充研究后再做，或直接说明“证据不足，暂不下结论”；不得为了满足默认时长而虚构、推断或堆砌废话。
- `preflight.md` 必须新增“有效信息密度”项：逐章写明新增的信息点与对应来源/判断。任一章节不能列出新增信息点，即为不通过，删除或重写后才能进入配音和渲染。

## 3. 事实研究口径

凡涉及当前市场、公司、ETF、政策、财报、价格、资金流、监管进展、宏观数据，必须先查证，不能凭记忆写脚本。

### 3.1 独立判断与反迎合门槛（强制）

开始研究、写脚本或生成标题前，必须先检查用户给出的命题、问题和预设结论，而不是默认它们成立。目标是形成可证伪的独立判断，不是迎合用户已有立场。

必须逐项检查：

- **错误前提**：主体、时间、状态、数字、范围或定义是否有误；问题是否把尚未发生、尚未证实或不存在的事情当成事实。
- **逻辑跳跃**：相关性是否被写成因果；局部样本是否被外推到全局；单日变化是否被解释为长期趋势；技术可行是否被等同于商业成功。
- **信息缺失**：是否遗漏基准数据、比较对象、时间窗口、统计口径、样本规模、政策阶段或关键上下文。
- **证据分层**：正文和事实底稿必须明确标记 `已确认事实 / 媒体报道或合理推测 / 主观观点与判断`，不得混写成同等确定性。
- **来源核验**：涉及数字、人物言论、机构行为和方向性结论时，尽量回到一手来源，并用独立可靠来源核对；无法直接核实时必须明示证据缺口。
- **反方与替代解释**：主动寻找反证、相反数据和至少 `1` 个能解释同一现象的替代原因；不能只收集支持原命题的材料。
- **遗漏变量与成本**：主动提醒用户可能忽略的变量、机会成本、交易与税务成本、时间成本、流动性成本、执行限制和二阶影响；不适用的项目可以标记“不适用”，不能无检查地省略。
- **认知与样本偏差**：检查确认偏误、幸存者偏差、选择性时间窗口、基数效应、事后归因、热点偏差和小样本过拟合。

如果不同意用户的前提或结论，必须直接指出，并给出：`依据 / 风险 / 替代解释 / 什么证据会改变当前判断`。不得先顺着错误前提生成内容，再只在结尾加一句免责声明。

每期 `preflight.md` 必须增加“独立判断审查”表，至少包含：`原始命题 / 前提是否成立 / 事实 / 推测 / 主观观点 / 反方证据 / 替代解释 / 遗漏变量 / 成本 / 偏差 / 失效条件 / 修改动作`。任一核心项未检查，或核心前提不成立却未修改标题和脚本，均为不通过，不得进入配音和 MP4。

每个项目必须建立 `product-facts.md` 或同等事实底稿，至少包含：

- 资料截止日期，例如 `资料截止：2026-07-28`（仅写入内部事实底稿，不直接进入成片或公开发布包装）
- 核心数据和单位，例如 `US$M`、`TWh`、`MW`、`BkWh`
- 数据方向，例如 `净流入 +246.0M`、`净流出 -465.2M`
- 关键来源链接
- 明确区分“官方披露”“媒体报道”“我的判断”
- 对可能变化的数据注明“最新可见日期”
- 记录核心命题的反方证据、替代解释、遗漏变量、成本与潜在偏差

金融类判断必须保持克制：

- 不把“可能、倾向、情景”写成确定结论
- 不用“必涨”“暴富”“梭哈”“稳赢”
- 不把单日数据当长期趋势
- 必须给出风险、失效条件和观察指标
- 不因用户偏好某个结论而降低证据标准；证据不支持时直接反驳或收缩结论

## 4. 脚本结构

默认分别生成 `script-shorts.md` 和 `script-long.md`。兼容旧项目时可以保留 `script.md` 作为长视频脚本，但新项目不得只写一份脚本再机械裁切成 Shorts。两份脚本都必须包含 YAML 头，并使用同一个 `product-facts.md` 作为事实底稿。

Shorts 固定结构：

1. `0—2 秒 hook`：直接给反常识结果、风险或关键冲突。
2. `2—12 秒 answer`：给出最小完整答案，不能只吊胃口。
3. `12—35 秒 proof`：只选 `1—2` 个最强证据或机制。
4. `最后 3—5 秒 bridge`：明确长视频多提供什么，并指向对应长视频。

长视频使用以下 YAML 头：

```yaml
---
title: 视频标题
cutoff: 2026-07-28
voice: zh-CN-YunyangNeural
rate: +3%（新手教程、概念课、长篇操作指南使用 +0%）
pitch: -2Hz
template_source: /Users/machao/Documents/Codex/2026-06-14/bitget/outputs/财经视频引擎.md
pronunciation_rule: 关键读法说明
visual_rule: 每段旁白都匹配对应数据图、解释图或主题背景
subtitle_numbers: 所有字幕和数据画面中的数字统一使用阿拉伯数字
duration_policy: 时长由证据与有效信息决定；禁止为达到目标时长填充重复或空泛旁白
---
```

默认结构：

1. `opening`：先说结论，3 句话内讲清楚核心判断
2. `data-source` 或 `facts`：数据来源、时间口径、单位
3. `data`：关键数据、趋势、对比
4. `mechanism`：机制解释，为什么这个数据会影响市场或股票
5. `winners / comparison / market-impact`：主要对象分层、受益链、影响链
6. `risk-filter`：风险、反例、失效条件
7. `scenarios`：基准、乐观、风险 3 种情景
8. `action`：普通投资者怎么观察、分批、控制仓位
9. `closing`：最后 3 句话总结、失效条件和风险提示；不口播或展示当天日期/资料截止日期

每段旁白必须使用 cue：

```markdown
## opening
[[cue:hook]]先说结论：……
[[cue:map]]这一期重点讲 3 件事……
```

## 5. 字幕和数字规则

- 所有字幕和数据画面中的数字使用阿拉伯数字
- 正确示例：`2026-07-28`、`13`、`+19.6M`、`-465.2M`、`3—5 天`
- 不要写成：`二零二六年`、`十三天`、`三到五天`
- **公开视频金额中文化（强制）**：标题、画面数据卡、字幕、旁白稿、缩略图、说明和标签中的美元金额直接写为中文金额，例如 `3亿5900万美元`、`41亿1000万美元`、`净亏损3亿5946万8000美元`。不得在公开画面只显示 `US$359M`、`US$4.11B`、`$24.2B` 等英文单位写法。内部事实底稿可保留“`3亿5900万美元（US$359M）`”双口径，供来源核验。
- 数据图必须标注：日期、单位、正负方向、来源；来源应放入必要的主证据板，不得做成右下角常驻角标
- 画面字幕不要超过 `2 行`；复杂观点拆成多段 cue
- 字幕安全区固定为画面底部 `260px`：默认仅供字幕使用。主视觉、人物、币种图案、数据卡、图表和装饰元素均不得进入此区域；先在上方完成布局，再叠加字幕。必要免责声明只在对应语义处短暂出现，不得常驻角落。
- 字幕默认使用无底框白字、深色描边或阴影，不能使用大面积白色或浅色卡片遮挡画面；安全区应保持干净。

## 6. 固定发音规则

默认发音：

- `BTC` 读作“比特币”
- `ETH` 读作“以太坊”
- `DeFi` 配音连续读作“迪发艾”（英文发音 /ˈdiːfaɪ/，dee-fye），中间不得加空格或停顿；字幕保留 `DeFi`，不得读成“得飞”或“迪菲”
- **固定词组与句子连读（强制）**：配音必须按语义块连读，不能把字幕逐字边界当作停顿点。`梭哈`、`空头挤压`、`美元储备`、`资金库`、`现货用户`、`链上授权`等固定词组不得在词内断开；例如“梭哈”必须作为一个完整词读出。
- 一个 cue 是完整陈述时，应保持自然连贯的句内语流，只在标点、明确转折或语义段落处短暂停顿；不得因缩写替换、数字替换或字幕换行造成不必要的断句。
- cue 不得从固定词组中间切开。若当前男声无法稳定连读，字幕保留术语，配音必须替换为语义等价且自然连贯的表达，并把替换记录在 `pronunciation_rule` 和 TTS 替换表中；不得交付词内明显停顿的音频。
- `ETF` 读作“E T F”
- `Q1` 读作“第 1 季度”
- `Q2` 读作“第 2 季度”
- `Q3` 读作“第 3 季度”
- `Q4` 读作“第 4 季度”
- `USDT` 读作“U S D T”
- `USDC` 读作“U S D C”
- `TRC20` 读作“T R C 二零”
- `ERC20` 读作“E R C 二零”
- `BEP20` 读作“B E P 二零”
- `AI` 读作“A I”
- `PPA` 读作“P P A”
- 3—4 个大写英文字母如果是公司名、股票代码或常见简称，配音优先读中文常用名；只有没有明确中文读法时，才逐字母读。

常见公司/股票简称配音读法：

- `ASML` 读作“阿斯麦尔”
- `AMAT` 读作“应用材料”
- `LRCX` 读作“拉姆研究”
- `KLAC` 读作“科磊”
- `NVDA` 读作“英伟达”
- `TSLA` 读作“特斯拉”
- `GOOGL` / `GOOG` 读作“谷歌”
- `META` 读作“Meta”
- `MSFT` 读作“微软”
- `AAPL` 读作“苹果”
- `QCOM` 读作“高通”
- `INTC` 读作“英特尔”
- `AMD` 读作“A M D”
- `ARM` 读作“Arm”
- `TSM` 读作“台积电”
- `BABA` 读作“阿里巴巴”
- `KO` 读作“可口可乐”
- `SPX` / `SPY` / `VOO` 读作“标普 500”
- 纯 ETF、指数、产品代码没有中文常用名时，逐字母读，例如 `CEG` 读作“C E G”、`PWR` 读作“P W R”

财报金额读法：

- 公开字幕、图表和标题直接使用中文金额，例如 `3亿5900万美元`、`41亿1000万美元`、`每股亏损1.36美元`；内部事实底稿保留原始披露写法并与中文换算并列。
- 男声不得逐个读符号或英文字母。所有 `US$ + M/B` 金额均为强制换算规则：`M` 先换算为“百万美元”，`B` 先换算为“十亿美元”，再统一读成中文“亿 / 万美元”。
- 固定读法：`US$359M` 必须读作“3亿5900万美元”；`US$4.11B` 必须读作“41亿1000万美元”；`US$8.22B` 必须读作“82亿2000万美元”；`US$1.220B` 必须读作“12亿2000万美元”。所有同类金额按相同规则处理，不得读成“359 M 美元”或“4.11 B 美元”。
- 带负号的财报金额按语义读，不读“负美元”。例如 `-US$359.468M` 在净利润语境读作“净亏损3亿5946万8000美元”；`-US$1.36` 在 EPS 语境读作“每股亏损1.36美元”。
- `US$24.45` 读作“24.45美元”；百分比和小数按中文自然读法，例如 `-38%` 读作“下降38%”。

如果某期有特殊读法，必须写进 `script.md` 的 `pronunciation_rule`，并在 TTS 脚本里做替换，不要只改字幕。

脚本生成时要区分“字幕显示”和“配音文本”：

- 字幕可以保留 `ASML`、`AMAT`、`LRCX`、`KLAC` 等代码，方便观众识别股票。
- 配音文本必须替换成中文读法，例如字幕显示 `ASML`，男声实际读“阿斯麦尔”。
- 如果同一个简称在不同语境下有不同读法，以当期 `script.md` 的 `pronunciation_rule` 为准。
- “转错链/转账”这类词的字幕可保留原文；若当前男声对“转”出现多音字误读，配音文本改为“发错网络/发送资产”，不要用错误声调强行朗读。

## 6.1 互动规则

- 每条视频最多只能有 `1` 个互动话题；不得在不同章节重复提问、投票或引导评论。
- 唯一互动话题必须放在视频最后 `1min`，位于结尾总结之前或紧邻总结；不得占用开头、数据解释或核心判断段落。
- 互动必须直接对应本期主题中的一个可判断问题，例如本期的“哪类风险最值得持续跟踪”；禁止“你怎么看”“评论区聊聊”等泛互动。
- 互动题保持 `A / B / C` 或 `A / B / C / D` 的单选格式，明确要求观众只回复字母；不得包含价格喊单、买卖建议或情绪煽动。
- `visual-plan.md` 必须标注唯一互动 cue 与预计所在的最后 `1min`；演示和最终时间轴都要验证不存在其他互动卡。

## 7. 视觉规则

每一段文字脚本都必须根据旁白内容生成对应视觉素材：

- 数据类旁白：趋势图、柱状图、对比卡片、资金流表格
- 观点类旁白：因果关系图、机制图、分层示意图
- 风险类旁白：红色风险卡、过滤器、情景图
- 建议类旁白：观察清单、仓位篮子、行动步骤
- 转场和停顿：与本段主题一致的背景图，不用无关装饰图

### 7.1 V2 证据板式画面规则（强制）

本规则学习的是“用画面证明观点”的方法，不复制任何参考视频的素材、标题、版式、配色、镜头或截图。

- 每个 cue 的版式分为 `3` 层：**氛围背景层**（自然背景）、**证据前景层**（数据/图示/步骤/对比）、**文字引导层**（短结论与来源）。背景不能替代证据前景层。
- 开场 `0—30` 秒必须至少出现 `1` 个“结论 + 关键数字/事实 + 机制或对象关系”的证据板。它要直接兑现标题，而不是只放频道名、风景或泛问题。
- 事实类 cue：优先使用自制的数字条、时间线、前后对比或来源卡；机制类 cue：优先使用因果链、路径图或分层图；行动类 cue：优先使用编号步骤、检查清单或决策树。无法安全展示真实素材时，使用自制抽象示意图，不用伪造截图。
- 证据前景层的核心对象必须放在字幕安全区上方，与标题形成明确阅读路径；同一时期不能让背景、图表、标题和字幕争夺视觉中心。
- 画面变化不只换背景。相邻 cue 至少改变以下 `1` 项：证据类型、信息结构、主对象位置、数据焦点或动效方向；新结论优先改变 `2` 项。
- 风险 cue 可用红色作“警示点”，但只用于风险数字、边框、箭头或结论词；不得以满屏红色、爆炸、恐慌表情等方式制造超出证据的情绪。
- 每个关键结论的来源在 `visual-plan.md` 标明。若来源只支持“媒体报道”或“待厂商确认”，画面同样必须把证据等级写清，不能把不确定项包装成官方定论。

证据板验收问题：观众静音看 `2` 秒，能否同时看懂“在讲什么、为什么重要、证据是什么”？任一答案是否，重做该 cue 的前景信息结构。

### V2 固定背景图库调用规则（强制，写死）

- 唯一背景图库固定为：`/Users/machao/Documents/Codex/2026-06-14/bitget/outputs/视频背景图合集`。
- 所有财经视频引擎视频只能从该文件夹现有的 `116` 张去重背景图中随机调用；**禁止使用 ImageGen 或任何图片生成工具新生成视频背景图**。
- 同一自然日按 `Asia/Shanghai` 计算，所有财经视频引擎项目全局不得重复使用同一张背景图；不是只在单个视频内去重。
- 每次制作在视觉计划前必须运行统一选择器，不得手工绕过：

```bash
python3 /Users/machao/Documents/Codex/2026-06-14/bitget/outputs/视频背景图合集/随机选择背景图.py \
  --project /绝对路径/outputs/当前视频项目 \
  --count 需要的背景图数量
```

- 选择器把图片复制到当前项目的 `backgrounds-selected/`，并生成 `backgrounds-selected.json`；该文件是本期背景来源的唯一准绳。
- 全局去重记录固定写入 `视频背景图合集/每日使用记录.tsv`。同一项目同一天重复执行时复用原选择，不额外消耗图库。
- 当天剩余图片少于需求时，必须停止并报告“图库不足”；不得重复旧图、跨日伪造日期或临时生成新图。
- 随机选择后，允许在 `visual-plan.md` 中按旁白语义重新排序已选图片，但不得换入未登记图片。
- 背景只负责氛围；事实、数据、机制、风险与行动仍由前景证据板表达。
- 风险段优先从已选图片中使用低饱和、留白较多的画面，不能用灾难、血红、爆炸等恐吓构图。
- 每张背景在 `visual-plan.md` 中记录：对应 cue、图库相对路径、选择日期、选择理由和前景证据类型；不再记录生成提示词。

禁止：

- 一张通用封面贯穿全片
- 一种底色、一个网格或一张背景图贯穿全片
- 与旁白无关的 AI 科技背景
- 画面继续停留在上一段无关内容
- 用无来源的假数据装饰
- 滥用 Logo 拼贴、行情截图、水印

背景图要求：

- 背景服务叙事，不遮挡字幕
- 主体尽量放右侧或中上部；画面底部 `260px` 为字幕安全区，不得放 BTC/ETH 图案、数据卡、图表、装饰或主体元素
- 数据卡片字体要足够大，手机小屏可读
- 画面出现新对象、新数据、新判断或新情景时，必须同步切换或有明显动效变化

## 8. 视觉对应表

每次生成视频前必须先做 `visual-plan.md`，把脚本拆成对应表：

| 章节 | 旁白重点 | 数据/画面 | 来源标注 |
|---|---|---|---|
| opening | 核心结论 | 主视觉 + 2 个核心数字 | 不显示制作日、上传日或当天日期 |
| data | 关键数据 | 柱状图/趋势图 | 官方来源 |
| mechanism | 机制解释 | 流程图 | 综合判断 |
| risk | 风险过滤 | 风险卡/过滤器 | 来源 |
| closing | 3 句话总结 | 总结清单 | 失效条件 + 风险提示，不显示当天日期 |

V2 版 `visual-plan.md` 必须增加以下列：

| 章节 / cue | 旁白重点 | 数据 / 画面 | 图库相对路径 | 选择日期 | 选择理由 | Hero 对象与状态变化 | 前景证据类型 | 来源标注 |
|---|---|---|---|---|---|---|---|---|

关键事实、机制、风险与行动 cue 还必须在“数据 / 画面”栏明确写出：证据前景类型、主结论、可核验数字/事实及辅助信息数量，确保渲染前即可检查“一屏一结论”。

`Hero 对象与状态变化` 栏必须写明本 cue 对同一对象做了什么变化，例如“凭证：展示价格 → 连到托管方 → 标记不可赎回风险”或“订单：订单页 → 暴露用户画像 → 连到钓鱼入口”。若本 cue 不使用 Hero 对象，必须注明理由；长视频中连续 `2` 个主要章节不使用任何 Hero 状态推进，视为不通过。

演示中必须逐段核对：

- 画面是否跟旁白一致
- 数据是否与事实底稿一致
- 字幕是否太长
- 是否出现长时间无变化
- 相邻背景图是否重复，是否存在同一底色贯穿全片
- 是否通过统一选择器从固定背景图库调取，并在 `每日使用记录.tsv` 登记
- 同一自然日的所有财经视频引擎项目是否没有重复使用同一张图
- 是否完全没有调用图片生成工具新生成背景图
- 已选背景是否在最终抽帧中实际可见，而非被不透明组件盖住
- 每个 Hero 对象是否在至少 `3` 个章节可辨认地延续，并有至少 `3` 次与旁白对应的状态变化
- 是否存在同一张整页卡片、曲线或静态 UI 连续停留超过 `12秒`；如存在，是否已拆成对象推进、数据聚焦或机制关系变化

## 9. 演示版要求

演示版默认用 HTML，文件名包含 `demo.html`。

演示版可以出现控制条，方便用户预览，但控制条必须只存在于演示页。正式 MP4 必须隐藏：

- `.controls`
- `.hint`
- “演示”
- “继续播放”
- “含声音”
- “测试”

演示交付时必须说明：

- 演示地址或 HTML 文件路径
- 脚本文件路径
- 事实底稿路径
- 视觉对应表路径
- 是否已有男声配音

### 9.1 演示合成后复合复查（强制）

HTML 演示完成不等于视觉验收完成。所有背景、主视觉、Logo、数据卡、字幕和控制层合成到同一画布后，必须重新播放并抽帧复查一次；不能只检查各组件单独显示时是否正常。

至少检查开头、每次主要布局变化、字幕最长的 cue、`50%` 时点、唯一互动和结尾。发现以下任一问题，必须修改并重新完成同一轮复查，直到 `preflight.md` 记录为“通过”：

- 右上角、左上角或画面边缘出现 Logo、粒子、人物、标题、卡片互相压住或被裁切
- 字幕与主视觉、数据卡、来源、免责声明重叠，或字幕超过 `2` 行、超出底部 `260px` 安全区
- 相邻字幕、标题、背景或卡片在转场帧中双层叠加、出现残影
- 主结论、数字和辅助文字争夺同一个视觉中心，静音 `2` 秒无法读清“一屏一结论”
- 演示控件、进度条、时间码、章节号、日期、来源角标或调试文字进入正式画面层

横版存在右侧 Hero、Logo 或人物时，必须使用互斥栏位：正文/证据栏与右侧视觉栏不得共享坐标区域，静态落位后边界至少保留 `24px` 可见间距。不能用“元素语义上有关联”解释视觉遮挡；粒子进入门、箭头指向卡片等关系也必须通过轨迹和留白表达，不得压在 Logo、标题、数据卡或字幕上。

复查不能只看 `3` 张固定抽帧。除开头、`50%` 和结尾外，还必须覆盖所有场景边界前后各 `1` 帧或各 `0.1—0.2 秒`，专门检查转场叠加；字幕最长的 cue 必须单独抽帧检查。

## 10. 配音和时间轴

男声配音完成后必须生成：

- `_narration-male/voiceover.mp3`
- `_narration-male/voiceover.wav`
- `_narration-male/timeline.json`
- `_narration-male/chunks/`

`timeline.json` 是最终视频节奏的唯一准绳。最终渲染必须按时间轴逐帧 seek，不依赖浏览器实时播放。

- 字幕按第0.2节使用最终音频对齐的独立短句 `start/end` 区间；场景时间轴包含cue间停顿，禁止仅累计语音duration造成漂移。连续字幕可延续至下一条开始，长停顿可留空；不得把整个旁白chunk当一屏字幕或按字数比例估算正式同步。
- 配音语速发生变化时，必须清除或隔离旧语速的分段音频缓存，重新生成配音、`timeline.json` 和正式视频；禁止复用旧时间轴。

必须避免的错误：

- 页面 `fetch timeline.json` 失败导致全片停在第 1 页
- 时间轴没注入就开始录制
- 中段和结尾抽帧仍是同一页
- TTS 读法只改了字幕，没有改配音文本
- TTS 中断后重复生成全部片段，浪费时间

TTS 脚本应支持：

- 已有有效 `wav` 自动跳过
- 单段失败自动重试
- 无效半截 `mp3` 不当作成功
- 输出总时长、场景数、chunk 数

### 10.1 Hyperframes 式可寻址时间轴（强制）

HTML 演示和正式渲染必须把视频看成“给定时间 `t` 就能得到唯一画面”的有限合成，而不是录制一次浏览器自动播放：

- `timeline.json` 继续以真实配音的 `absoluteStart` 和 `totalDuration` 为唯一节奏源；脚本估时、CSS 动画时长和浏览器播放进度不得反向覆盖真实时间轴。
- 所有场景、字幕、数据卡和关键媒体必须有稳定、唯一的 ID，并记录开始时间和持续时间。轨道时间可为连续Hero或音频衔接重叠，但旧新场景不得双显，字幕不得交叉叠加；视觉前后层由 `z-index` 决定，轨道编号只用于组织，不得误当作绘制层级。
- 页面必须提供 `window.__seek(t)`；在相同素材、字体、数据和时间轴下，多次调用同一个 `t` 必须得到相同画面。正式渲染按 `frame / fps` 计算 `t` 后逐帧 seek，不使用屏幕录制代替。
- 页面完成 DOM、字体、图片、SVG、时间轴和首帧布局后才设置 `window.__ready = true`。渲染器必须等待 ready；等待超时、资源加载失败或控制台出现未处理错误时停止合成。
- 正式渲染设置 `window.__recording = true`。该状态下禁止自动播放、自动循环、鼠标悬停、滚动触发和预览控制条；所有变化只能由 seek 驱动。
- 禁止把 `Date.now()`、`performance.now()`、未设种子的 `Math.random()`、`setTimeout`/`setInterval`、无限动画、异步创建的关键时间线或未注册的 `requestAnimationFrame` 用于正式画面状态。
- 动效必须有限且有终态。关键数字、图表、机制链和结论卡需留出可读停留；除非叙事明确要求，不得结尾归零、跳回初始态或黑屏。

采用 Hyperframes 时，按其原生合成契约执行：根节点声明 `data-composition-id`、`data-start="0"`、显式画布尺寸与有限 `data-duration`；定时元素声明稳定 `id`、`data-start`、`data-duration`；GSAP 使用 `paused: true` 的有限时间线，并以合成 ID 为键同步注册到 `window.__timelines`。没有安装 Hyperframes 时，也必须实现上面的 `__ready / __seek / __recording` 等价契约。

媒体时间由同一时间轴接管。采用 Hyperframes 时，不得在合成脚本中自行调用媒体 `play()`、`pause()` 或随意写 `currentTime`；视频/音频裁切偏移统一使用 `data-media-start`。独立 HTML 实现也必须保证暂停 seek、跳跃 seek 和逐帧 seek 后的画面、字幕与音频位置一致。

### 10.2 连续运动叙事与组件复用（强制）

- 长视频不是逐页 PPT。围绕本期主问题选择 `1—2` 个跨章节持续存在的 Hero 元素，例如同一条估值曲线、同一家公司卡片、同一条资金传导路径或同一组对比柱；通过重排、聚焦、缩放、遮罩和标注推进解释，保持对象身份连续。
- Hero 元素必须是本期内容中的具体对象或关系，不得用“标题卡”“通用网格”“装饰粒子”“无数据曲线”冒充连续叙事。若使用曲线、卡片或流程图，至少要在后续章节改变其数据焦点、关系节点、风险状态或可执行出口；仅替换标题、数字或整页文案不算对象推进。
- 每个 Hero 的状态必须在时间轴中可寻址：使用稳定 DOM/组件 ID，并在 `visual-plan.md` 记录首次出现、每次变化和结束时点。正式渲染前对各状态时点重复 seek `2` 次，画面必须一致；正式成片抽帧必须能看出同一对象的连续故事线。
- 每个主要章节至少产生 `1` 项可见动态：Hero 状态变化、数据焦点推进、对象关系重连、遮罩揭示、风险标注或行动出口。禁止把 `40—80秒` 的章节做成背景不动、整页卡片不动、只有字幕变化的静态承载区。
- 必须记录最终视频码率、时长、文件大小和编码设置；大小和码率仅用于技术排查，不作为信息密度或质量通过线。实际检查文字边缘、运动、压缩失真、对象推进与证据可读性；静态冗余先改叙事，编码失真再调整参数，禁止只增大码率制造虚假高质量母版。
- 将字幕、来源条、数字卡、趋势图、机制节点、风险卡、转场和 Logo 安全区做成可复用组件；复用组件结构和动画语法，不复用无关结论、旧数据或旧来源。
- 动效优先使用 `transform`、`opacity`、遮罩、SVG 路径和明确的状态切换；避免用频繁修改布局尺寸、临时增删 DOM 或纯装饰粒子承担核心论证。
- 固定背景图库规则继续生效。Hyperframes/HTML 只负责合成和前景运动，不得借此绕过图库选择器、生成新背景或重复使用当天已登记背景。
- 演示页可按窗口等比缩放并 letterbox，但内部合成始终按 `1920×1080` 或 `1080×1920` 固定坐标渲染；缩放不得改变换行、元素位置或时间轴。

## 11. MP4 合成规则

正式 MP4 合成前：

- 确认 `preflight.md` 的脚本门和有声演示门均通过，并取得用户合成确认；渲染后再完成成片门及发布门。未通过项先修复，不得预填成片通过或把测试渲染交付为正式成片。
- 确认 `timeline.json` 已生成
- 确认 `voiceover.mp3` 可用
- 确认 HTML 支持 `window.__seek(time)`
- 确认 `window.__ready === true` 后才开始抓帧，且正式抓帧期间 `window.__recording === true`
- 确认渲染脚本会隐藏演示控件
- 如果页面依赖时间轴，渲染脚本必须直接注入 `timeline.json`
- 在开头、中段、场景边界和结尾各选至少 `1` 个时间点连续 seek 两次，确认同一时间得到同一画面；存在随机装饰时必须固定种子

正式 MP4 合成后必须用 `ffprobe` 验证：

- 视频流存在
- 音频流存在
- 分辨率为 `1920×1080`
- 帧率为 `25fps`
- 视频编码为 `H.264`
- 音频编码为 `AAC`
- 时长接近 `timeline.totalDuration`

还必须完整解码视频与音频到结尾，监听旁白可懂度并检查至少一个有旁白的 BGM 段落；不能只凭 `ffprobe` 有音频流就判定混音通过。若使用 Hyperframes，则先运行其当前项目支持的 `lint` 和 `check`，再做本模板的完整解码、抽帧和听检；框架检查不能替代成片验收。

正式字幕、配音、BGM、Logo 与画面完成最终复合后，必须再执行一次“最终复合复查”。HTML 演示阶段已经通过不能替代本次检查，因为字体渲染、字幕断行、视频缩放、编码裁切和混流时间轴都可能在最终合成后产生新叠加问题。最终复查不通过时，必须修复源 HTML、字幕切分或渲染参数并重新合成；禁止只在验收记录中说明问题后直接交付。

必须抽帧检查：

- 开头，例如 `00:00:05`
- 中段，例如总时长 `50%`
- 结尾，例如结束前 `10—15 秒`

抽帧验收标准：

- 3 张画面不是同一页
- 没有演示控件
- 没有“演示”“继续播放”“含声音”
- 字幕不被遮挡
- Logo、粒子、人物、标题、证据卡和字幕之间没有遮挡、压字、贴边裁切或右上角叠加
- 必要的数据来源放在对应证据板中；不得出现右下角常驻来源、右上角章节数字、角落提示或底部进度条
- 场景切换抽帧没有双层背景、双份文字或字幕残影；制作日、上传日和当天日期不可见
- 画面内容和对应旁白一致
- 同一时间点重复抽帧的核心像素一致；场景边界前后帧没有旧组件残留、双层文字、闪黑或媒体跳帧

## 12. YouTube 发布物料

最终 MP4 生成并核验通过后，必须同时交付：

- `3` 组一一对应的候选标题，每组包含 Shorts 标题和长视频标题
- `1` 份 Shorts 说明和 `1` 份长视频说明
- 两组标签和 YouTube 关键词；每条只保留高度相关关键词
- `1` 张 Shorts 封面和 `1` 张长视频缩略图
- Shorts 的相关视频、说明链接和置顶评论均指向对应长视频

**对话交付规则（强制）：** 不得只给出 MP4 或目录路径。最终回复必须在同一条消息内直接列出可复制发布的首选标题、Shorts 说明、长视频说明、标签、关键词和置顶评论；同时附上对应文件与缩略图路径。若长视频尚未发布，链接位置必须明确写成“发布后替换为长视频 URL”，不得编造链接。

标题要求：

- 围绕视频实际主题
- 清楚体现对象、数据或核心问题
- 不夸大、不恐吓
- 不把情景判断写成确定结论

说明必须包含：

- 内部保留资料截止日期；公开说明不标记制作日、上传日或当天日期
- 核心数据
- 主要判断
- 股票池或资产池
- 章节时间轴
- 数据来源
- 风险提示

发布物料中不得出现：

- “演示版”
- “测试版”
- “继续播放”
- “含声音”
- “我刚生成”
- “你确认后”

## 13. YouTube 缩略图要求

默认生成 `1280×720` PNG。

缩略图必须根据视频脚本和最终标题生成：

- 一眼看出视频讨论对象
- 只保留少量必要文字
- 数据数字使用阿拉伯数字
- 突出核心数据、资产或对比关系
- 颜色和结论一致
- 手机小尺寸可读

禁止：

- 无关人物
- 无关行情截图
- 错误数据
- Logo 堆砌
- “暴涨”“归零”“崩盘”等未经内容支持的词

## 14. 默认交付清单

完成一个项目后，最终目录至少包含：

- `preflight.md`
- `product-facts.md`
- `title-pairs.md`（3 组对应标题与首选方案）
- `script-shorts.md`
- `script-long.md`
- `visual-plan-shorts.md`
- `visual-plan-long.md`
- `*-shorts-demo.html`
- `*-long-demo.html`
- `_narration-shorts/timeline.json`
- `_narration-shorts/voiceover.mp3`
- `_narration-long/timeline.json`
- `_narration-long/voiceover.mp3`
- `*-shorts.mp4`
- `*-long-youtube.mp4`
- `youtube-metadata-shorts.md`
- `youtube-metadata-long.md`
- `youtube-thumbnail-shorts.png`
- `youtube-thumbnail-long.png`
- `final-frame-start.png`
- `final-frame-mid.png`
- `final-frame-end.png`

最终回复用户时必须简洁列出：

- MP4 路径
- 缩略图路径
- YouTube 文案路径
- 验证结果
- 如有偏离模板，例如时长超过 `8 分钟`，必须说明

## 15. 质量底线

不能交付的情况：

- MP4 没有声音
- 音频不是男声
- 中段/结尾抽帧仍停在首页
- 正式 MP4 带演示控件
- 字幕大面积遮挡数据
- 数据没有来源
- 标题和视频内容不一致
- 缩略图与视频主题无关
- 没有 YouTube 发布物料
- 主要章节仍使用同一底色、同一网格或同一背景图，或未通过固定图库选择器完成当日去重
- 没有字幕 或打开MP4 看不到字幕

如果发现上述问题，必须先修正再交付，不要让用户上传后再发现。

## 16. V2 的 6章节＋7组件直调规则（强制）

长视频必须从 `财经视频组件库/six-section-template` 起步，按 `01-opening / 02-facts / 03-mechanism / 04-countercase / 05-scenarios-action / 06-closing` 生成 6 个可独立 seek 的章节。Shorts 不凑 6 章，只调用能在 25—60 秒内证明核心结论的 1—2 个组件。

组件调用矩阵：

- 估值/价格分位→`valuation-curve`；财报好坏或股价反应→`earnings-expectation-gap`。
- 解释为什么→`causal-flow`；检验单边叙事→`bull-bear-comparison`。
- 未来分支→`three-scenarios`；观众下一步→`action-checklist`。
- 所有字幕、来源等级、统计口径、示意/AI 声明→`caption-source`。

脚本、`visual-plan-long.md` 和宿主 `index.html` 必须使用同一组件 ID。本期数据只能由 `product-facts.md` 经 `data-variable-values` 注入；缺数据就删除组件或明示“证据不足”，不得保留示例数字。

每期 `preflight.md` 新增“6章节组件路由”表：章节、cue、组件 ID、注入变量、来源、证据等级、触发条件、失效条件。完成后必须先扫描占位符/旧数据，再执行 `check`、6 章中点 snapshot、关键对象 keyframes 诊断和 animation-map 审核；`--strict-variables` 只加在带变量的正式 `render` 命令上。

时间轴遵守 HyperFrames Core：子合成样式/主体/脚本全在 `<template>` 内，宿主 ID＝内部 ID＝时间线 key；每个合成只有 1 条暂停、有限、可 seek 时间线。媒体不手工 `play/pause/currentTime`，位移和缩放在非 timed 内层包装上做。长片保持 1—2 个跨章 Hero，不得变成整页切换的 PPT。

## 17. V2 的 Nuwa＋Cangjie 双蒸馏应用（按需强制）

本节学习并调用：

- Nuwa：`https://github.com/alchaincyf/nuwa-skill`——提炼创作者的选题镜片、推理习惯和表达 DNA。
- Cangjie：`https://github.com/kangarooking/cangjie-skill`——把长视频、访谈、播客和课程中的方法论编译成带来源、触发器、步骤和边界的方法卡。

它们只提供“学习层”，不能覆盖 V2 的独立判断、财经一手核验、Shorts/长视频分别创作、六章节、七组件、固定背景图库和最终质检。参考视频只能提供待验证的创作方法或研究线索，不能替代公司公告、财报、监管文件和权威数据。

### 17.1 自动路由

| 输入情况 | 执行 |
|---|---|
| 无创作者或参考视频 | 不触发双蒸馏，直接执行 V2 |
| `1` 条视频，用户要求读完、拆理论或学思路 | 执行 Cangjie；不得推断稳定人物风格 |
| 同一创作者至少 `3` 条代表视频 | Nuwa＋Cangjie；样本尽量包含热门、普通、近期各 `1` 条 |
| 只研究局部讲法或一个机制 | 最小范围 Cangjie，不扩大成人物画像 |

若当前环境已安装 `huashu-nuwa` / `cangjie-skill`，调用前必须完整读取对应 `SKILL.md` 和本任务直接引用的参考文件。若不可调用，按本节兼容契约执行并明确记录“兼容模式”，不得声称运行了外部 Skill 的脚本或验证器。

### 17.2 双蒸馏产物

项目必须创建：

```text
reference-learning/
├── source-manifest.md
├── source-notes.md
├── transcripts/
├── keyframes/
├── creator-method-dna.md
├── method-cards.md
├── rejected-methods.md
└── distillation-audit.md
```

- `source-manifest.md`：标题、作者/UP主、发布时间、URL、本地文件、获取日期和字幕类型。
- `transcripts/`：完整带时间戳字幕/转写；没有文本不得凭记忆蒸馏。
- `keyframes/`：开场、证据、图表、转折、反例和收尾的必要抽帧，不抓取无关画面。
- `source-notes.md`：逐项区分 `本人原话 / 来源支持归纳 / 外部事实 / 财经视频引擎推断 / 未确认`。

### 17.3 Nuwa 输出规范

Nuwa 分析以下稳定变量：选题冲突、观众问题、结论前置程度、证据顺序、类比密度、数据/叙事比例、转折方式、确定性语气、反方处理、行动收束和表达禁区。

每条写入 `creator-method-dna.md` 的规则必须包含：`观察 / 至少2个样本与时间戳 / 适用场景 / 反例或矛盾 / 财经视频引擎原创转译`。只在 `1` 条视频中出现的写法标记为“单样本观察”，不能升级为创作者 DNA。

Nuwa 的人物角色扮演不进入 V2：不得冒充创作者、复制口癖、标志句、声音或人格；最终成片保持财经视频引擎自己的表达身份。

### 17.4 Cangjie 方法卡规范

先完成整片的 `结构 / 解释 / 批判 / 应用`，再把候选方法写成：

| 字段 | 要求 |
|---|---|
| `R 来源` | 必要短引文或准确转述＋时间戳 |
| `I 解释` | 用财经视频引擎自己的话重写核心机制 |
| `A1 原案例` | 原视频怎样使用、结论和结果 |
| `A2 未来触发` | 什么选题、脚本或镜头问题才调用 |
| `E 执行` | 有完成标准和判停条件的步骤 |
| `B 边界` | 反例、失效条件、作者盲点、不适用场景 |

每张候选卡执行三重验证：

1. `跨段/跨作品复现`：至少 `2` 处独立证据；单视频不足以证明稳定风格。
2. `生成力`：能处理原视频未直接回答的新创作问题。
3. `独特性`：排除“标题要吸引、内容要有用”等空泛常识。

通过的进入 `method-cards.md`；未通过的进入 `rejected-methods.md`，保留淘汰理由，不得为了显得学到了很多而强行采用。

### 17.5 分别应用到 Shorts 和长视频

- 标题阶段：学习“冲突如何被发现”，但仍由 `标题V2.md` 生成原创的三组对应标题，不复制参考标题句式。
- Shorts：只选 `1` 张通过验证的方法卡，重写为“`0—2秒原创钩子 → 最小完整答案 → 1—2个证据/机制 → 具体边界或长视频承接`”。
- 长视频：最多选 `1—3` 张方法卡，映射到 `01-opening / 02-facts / 03-mechanism / 04-countercase / 05-scenarios-action / 06-closing`；反证与失效条件不得删除。
- 两条视频可以共享事实底稿与方法卡，但必须分别写脚本、视觉计划和时间轴；不得把同一段参考内容做横竖屏复刻。
- 视觉层只吸收信息顺序、证据优先级和节奏逻辑；不得复制原视频截图、素材、配色、版式、转场或独有镜头。

单期结果默认仅在当前项目有效。某条方法只有在至少 `3` 个独立来源或 `3` 次财经视频引擎项目中复现，并经用户确认，才允许晋级为模板级长期规则。

### 17.6 方法压力测试与验收

每张实际采用的方法卡至少包含 `3` 条 `should_trigger`、`2` 条 `should_not_trigger` 和 `1` 条 `edge_case`。至少一个负例必须应该触发另一张方法卡或原 V2 规则，用来检查方法之间是否抢调用。诱饵误触发为 `0` 容忍；总通过率低于 `80%` 时回炉重写 `A2/E/B`。

`preflight.md` 增加“Nuwa＋Cangjie 双蒸馏审查”表：

`来源 / 时间戳 / 观察或方法 / Nuwa或Cangjie / 三重验证 / 反例 / 原创转译 / Shorts或长视频 / 映射章节与cue / 是否采用 / 未采用原因`

`distillation-audit.md` 最后必须回答：

1. 哪些内容来自原作者，哪些是财经视频引擎推断？
2. 哪些规则通过跨作品验证，哪些只有单样本？
3. 有没有复制原句、独有口癖、声音、素材或画面结构？有则删除或重做。
4. 学到的方法是否提高了证据密度、解释清晰度或留存，而不是只让文案“听起来像某个人”？

## 18. V2 智能动画路由（默认自动执行）

调用“财经视频引擎V2.md”“财经视频引擎V2模板”或任何等价表达时，必须在完成标题、六章节和 `[[cue:id]]` 字幕后自动执行以下命令；用户不需要再次提出动画要求：

```bash
bash ./财经视频组件库/启用智能动画.sh /绝对路径/当前项目/script.md
```

命令输出 `motion-plan.json`、`motion-plan.md`、`animation-preflight.md`。三者缺一、命令非零或检查不通过时，流程停在脚本门，禁止开始HTML演示、男声成片和MP4。仅在文字计划里声称“有动态效果”不算执行。

### 18.1 按字幕语义选择动画语法

| cue语义 | 默认引擎与语法 |
|---|---|
| 资金流入、流出、融资、申购赎回、分配 | D3 `fund-flow`＋GSAP路径推进 |
| 营收、成本、利润、自由现金流桥 | D3 `cashflow-waterfall`＋GSAP归因揭示 |
| 同比、环比、价格、利润率、ETF每日数据、估值 | D3 `line-bar-reveal`＋GSAP数据焦点 |
| 因果、传导、诈骗、攻击、清算和风险路径 | GSAP SVG `risk-path` |
| BTC/ETH、多空、股票/凭证及其他同维对比 | GSAP `comparison-split`，Hero连续分裂/合并/对峙 |
| 事件发展、政策阶段、历史节点 | D3 `timeline-focus`＋GSAP焦点移动 |
| 教程、步骤、核验清单 | GSAP `tutorial-state-machine` |
| 多层复杂机制或关键高潮，且DOM/SVG难以讲清 | Motion Canvas `motion-canvas-shot` |

路由器必须读取标题、章节和完整cue旁白，不得只靠标题关键词。自动结果必须人工语义复核；复核包括语义类型、Hero身份、起止状态、数据证据、fallback和Motion Canvas必要性。误判时修改计划或脚本并重新运行检查。

### 18.2 强制生产门

- 长视频保持1—2个稳定ID的Hero，至少跨3章发生可见且与旁白对应的状态变化；背景、标题、数字或整页卡片替换不算Hero推进。
- 全片至少使用2种内容驱动的动画语法。全部采用同一种语法，或只有 `fade / slide / stagger`，直接不通过。
- 所有D3数据镜头必须绑定 `product-facts.md` 的具体字段、原始来源、日期、单位和统计口径；缺数据/缺来源时必须阻断，不得自动补数字。没有数值的关系图只能标“机制示意”。
- Motion Canvas不是每个cue的默认项，只用于关键复杂镜头；每次采用必须解释为什么GSAP/D3不足，并保持有限时长、确定性和可seek。
- GSAP时间线必须 `paused: true` 并以合成ID注册到 `window.__timelines`；D3负责真实数据几何，运动统一交给同一绝对时间轴。
- 固定背景图库继续作为长视频主画面；动画层不得用不透明底板长期盖住背景，也不得绕开每日不重复选择规则。
- 字幕继续由真实男声音频时间驱动；所有转场、Hero、D3与Motion Canvas镜头都必须通过Hyperframes确定性seek、重复渲染、状态抽帧和无重影检查。
- `preflight.md` 新增“智能动画路由”表：`cue / 完整语义 / 引擎 / 语法 / Hero起止状态 / 数据字段和来源 / Motion Canvas必要性 / 人工复核 / 实现证据 / 状态`。任何数据cue仍为 `missing` 或未绑定来源字段，不得进入演示门。
